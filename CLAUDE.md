## 编码要求
- 代码风格使用 Google 风格
- 注释使用中文, 但标点符号使用英文
- 必须使用 TypeScript 类型注解 (严格模式, 禁止隐式 any)
- 文件开头添加 `// -*- coding: utf-8 -*-` 注释

## 代码要求
- 尽量复用代码, 而不是重复造轮子
- 尽量使用 async/await 而不是回调函数
- 尝试新思路如果失败, 则需要还原这次改动然后再重新其他思路, 避免代码杂糅
- 业务逻辑要清晰, 功能尽量解耦, 避免功能严重耦合

## 依赖管理
- 使用 npm 管理依赖
- 运行代码: `node dist/cli.js` 或 `npm run build`
- 运行测试: `npm test`

## 项目管理
- 使用 git 进行版本控制
- 针对功能写单元测试, 保证测试通过
- 测试框架: Vitest
