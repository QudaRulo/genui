# genui 依赖管理说明

## 依赖分类

### 1. 核心依赖（必需）
定义在 `[project.dependencies]`，安装项目时自动安装：
- `langchain-core` - LangChain核心库
- `langchain-openai` - OpenAI集成
- `pydantic` - 数据验证
- `python-dotenv` - 环境变量加载
- `colorlog` - 彩色日志
- `rich` - 富文本输出

### 2. 开发依赖
定义在 `[tool.uv.dev-dependencies]`，仅开发时需要：
- `pytest` - 测试框架
- `pytest-asyncio` - 异步测试支持

### 3. 可选依赖
定义在 `[project.optional-dependencies]`，运行时可选功能：
- `anthropic` - Anthropic/Claude模型支持

---

## 使用方法

### 场景1: 用户安装（生产环境）

```bash
# 仅安装核心依赖
uv pip install genui

# 或者使用pip
pip install genui

# 如果需要Anthropic支持
uv pip install "genui[anthropic]"
```

### 场景2: 开发者环境

```bash
# 同步所有依赖（核心 + 开发依赖）
uv sync

# 查看安装了什么
uv pip list
```

**重要**: `uv sync` 会自动安装：
- ✅ 核心依赖（dependencies）
- ✅ 开发依赖（dev-dependencies）
- ❌ 不会自动安装可选依赖

### 场景3: 开发环境 + 可选依赖

```bash
# 方式1: 同步时指定（推荐）
uv sync --extra anthropic

# 方式2: 先同步，再安装可选
uv sync
uv pip install "genui[anthropic]"

# 方式3: 同步所有可选依赖
uv sync --all-extras
```

### 场景4: CI/CD测试环境

```bash
# 只安装核心 + 开发依赖（不含可选）
uv sync

# 运行测试
uv run pytest tests/
```

---

## 常见命令对比

| 命令 | 安装内容 | 使用场景 |
|------|----------|----------|
| `uv sync` | 核心 + 开发 | 日常开发（不需要Anthropic） |
| `uv sync --extra anthropic` | 核心 + 开发 + Anthropic | 开发时测试Anthropic |
| `uv sync --all-extras` | 核心 + 开发 + 所有可选 | 完整功能测试 |
| `uv pip install genui` | 仅核心 | 用户安装 |
| `uv pip install "genui[anthropic]"` | 核心 + Anthropic | 用户需要Anthropic |

---

## 验证安装

### 检查核心功能

```bash
uv run python -c "from genui import UIGenerator; print('✅ 核心功能正常')"
```

### 检查开发工具

```bash
uv run pytest --version
# 输出: pytest 8.x.x
```

### 检查Anthropic支持

```bash
uv run python -c "from langchain_anthropic import ChatAnthropic; print('✅ Anthropic已安装')"
```

---

## 依赖更新

### 更新所有依赖到最新兼容版本

```bash
# 更新并重新锁定
uv sync --upgrade

# 查看更新了什么
git diff uv.lock
```

### 更新特定包

```bash
# 更新pytest
uv pip install --upgrade pytest

# 重新锁定
uv lock
```

---

## pyproject.toml 结构说明

```toml
[project]
dependencies = [...]              # 核心依赖（必需）

[project.optional-dependencies]
anthropic = [...]                 # 可选运行时依赖

[tool.uv]
dev-dependencies = [...]          # 开发工具依赖（测试、lint等）
```

**关键区别**:
- `dev-dependencies`: 开发工具，用户不需要
- `optional-dependencies`: 运行时可选功能，用户可能需要

---

## 常见问题

### Q1: 为什么 `uv sync` 没有安装anthropic？

**A**: 因为anthropic是可选依赖，需要明确指定：
```bash
uv sync --extra anthropic
```

### Q2: 如何在开发时测试所有功能？

**A**: 安装所有可选依赖：
```bash
uv sync --all-extras
```

### Q3: CI中应该如何安装？

**A**: 根据需要：
```bash
# 基础测试（不含Anthropic）
uv sync

# 完整测试（含所有可选）
uv sync --all-extras
```

### Q4: 用户应该如何安装？

**A**:
```bash
# 基础安装
pip install genui

# 需要Anthropic
pip install "genui[anthropic]"
```

---

## 最佳实践

### 开发者

1. **日常开发**: `uv sync`
2. **测试Anthropic**: `uv sync --extra anthropic`
3. **提交前**: 确保 `uv.lock` 已更新

### CI/CD

```yaml
# GitHub Actions示例
- name: Install dependencies
  run: uv sync --all-extras  # 测试所有功能

- name: Run tests
  run: uv run pytest tests/
```

### 用户

```bash
# 最小安装
pip install genui

# 根据需要添加可选依赖
pip install "genui[anthropic]"
```

---

**总结**:
- 开发用 `uv sync`（自动含dev依赖）
- 可选功能用 `--extra <name>`
- 用户用 `pip install genui[extras]`
